#!/usr/bin/python
num = float(raw_input("Enter a number: "))
if num > 0:
	print "Number %r is postive number" % num
elif num == 0:
	print "Number is Zero" 
else:
	print "Number %r is negative number" % num